<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
  <title>Guti's blog</title>
</head>
<body>
  <div class="container p-5">
    <h1 class="mb-2"><%= article.title %></h1>
    <span class="text-muted d-block mb-2"><%= article.createdAt.toLocaleDateString() %></span>
    <div class="d-flex gap-2 mb-4">
      <a href="/" class="btn btn-secondary">Go home</a>
      <a href="/articles/edit/<%= article.id %>" class="btn btn-info">Edit blog</a>
      <form action="/articles/<%= article.id %>?_method=DELETE" method="post" class="d-inline">
        <input type="submit" class="btn btn-danger" value="Delete">
      </form>
    </div>

    <hr class="border border-2 opacity-25"/>

    <div id="blog" class="pt-4"><%- article.sanitizedHtml %></div>
  </div>
</body>
</html>